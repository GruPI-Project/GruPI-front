# Dockerfile para ambiente de desenvolvimento com hot reload
FROM node:18-alpine

# Instalar Bun para gerenciamento de packages
RUN npm install -g bun

# Definir diretório de trabalho
WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./
COPY bun.lock* ./

# Instalar dependências
RUN npm install

# Expor porta do Vite
EXPOSE 5173

# Comando para iniciar servidor de desenvolvimento
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]